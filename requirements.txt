# Core dependencies
sqlalchemy>=2.0.0
alembic>=1.10.0
requests>=2.28.0
python-dotenv>=1.0.0

# CLI and user interface
click>=8.1.0
rich>=13.0.0
prompt-toolkit>=3.0.0

# Mock data generation
faker>=15.0.0

# LLM integrations
openai>=1.0.0
anthropic>=0.7.0

# Database drivers
# sqlite3 is included in Python standard library
psycopg2-binary>=2.9.0  # PostgreSQL support
pymysql>=1.0.0          # MySQL support

# Development and testing
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
pre-commit>=3.0.0

# Monitoring and observability
structlog>=23.0.0
prometheus-client>=0.16.0

# Security
cryptography>=40.0.0

# Production WSGI server (optional)
gunicorn>=20.1.0
uvicorn>=0.20.0

# Optional: Advanced features
redis>=4.5.0           # For caching
celery>=5.2.0          # For background tasks
pydantic>=2.0.0        # For data validation

# RAG (Retrieval-Augmented Generation) dependencies
# Note: Install separately with: pip install sentence-transformers faiss-cpu
# sentence-transformers>=2.2.0  # For text embeddings
# faiss-cpu>=1.7.0              # For vector similarity search
# faiss-gpu>=1.7.0            # Alternative: GPU version for better performance